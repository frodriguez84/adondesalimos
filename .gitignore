# Dependencias
node_modules/

# Build
.next/
out/
dist/
build/

# TypeScript / Next
*.tsbuildinfo
next-env.d.ts

# Testing
coverage/

# Fuentes oficiales de los polígonos (15 MB). Se rebajan cuando hacen falta:
# los comandos están en data/zones/README.md. Lo versionado es data/zones/.
data/sources/

# Entorno
.env
.env.local
.env*.local

# Logs
*.log
npm-debug.log*

# Editor / SO
.DS_Store
Thumbs.db

# Claude Code — config local del usuario
.claude/settings.local.json

# Playwright MCP — snapshots/screenshots de QA en vivo (se regeneran por corrida)
.playwright-mcp/

# MCP local del proyecto (tooling de dev/QA; cada quien decide si lo activa)
.mcp.json
