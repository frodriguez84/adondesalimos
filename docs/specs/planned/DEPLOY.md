# DEPLOY — movido

El **primer tramo de código de F1** (el premium apagado: tabla `premium_interest`, endpoint,
`SuscripcionPanel` y el conteo en `/admin`) se implementó el 2026-08-01, así que el spec pasó a
**[`docs/specs/active/DEPLOY.md`](../active/DEPLOY.md)** — es normativo y sigue abierto: falta
F0 (Neon), el resto de F1 (`noindex`, `maxDuration`, `.env.example`, Vercel + DNS), F2 y la F3
gateada por interés medido (decisión 18).

- QA con IDs trazables: [`AnalisisQA.md`](../../qa/AnalisisQA.md) § *DEPLOY (el premium apagado)*
